\documentclass{minimal}
\usepackage[a4paper,margin=1cm,landscape]{geometry}
\usepackage{tikz}

%%%<
\usepackage{verbatim}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{5pt}%
%%%>

\begin{comment}
:Title:  Rule based diagram

\end{comment}

\usetikzlibrary{positioning,shadows,arrows}

\begin{document}
\begin{center}
\begin{tikzpicture}[
    fact/.style={circle, draw,
        text centered, anchor=north},
    state/.style={circle, draw,
        text centered, anchor=north},
    leaf/.style={circle, draw,
        text centered, anchor=north},
    level distance=0.5cm, growth parent anchor=south
]

\node (mul) [state] {\texttt{*}} [->] [sibling distance=4cm]
    child{ [sibling distance=2cm] 
        node (sin) [state] {\texttt{sin(}$\cdot$\texttt{)}}
        child{ 
            node (add) [state] {\texttt{+}}
            child{ 
                node (x1) [leaf] {\texttt{x}}
            }
            child{ 
                node (two) [leaf] {\texttt{2}}
            }
        }
    }
    child{ [sibling distance=2cm]
        node (pow) [state] {\texttt{\^~}}
        child{
            node (pha) [state] {\texttt{(}$\cdot$\texttt{)}}
            child{
                node (sub) [state] {\texttt{-}}
                child{
                    node (x2) [leaf] {\texttt{x}}
                }
                child{
                    node (one) [leaf] {\texttt{1}}
                }
            }
        }
        child{ 
            node (five) [leaf] {\texttt{5}}
        }
    }
;
        
\end{tikzpicture}
\end{center}
\end{document}
